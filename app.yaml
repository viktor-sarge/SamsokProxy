runtime: python312

handlers:
- url: /favicon\.ico
  static_files: favicon.ico
  upload: favicon\.ico

- url: .*
  script: main:app

automatic_scaling:
  max_instances: 2
  min_instances: 0
  max_idle_instances: 1
  min_idle_instances: 0

